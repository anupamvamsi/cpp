#include <iostream>

using namespace std;

main() // no need to use return statements, main() will assume that 0 is being returned
{
	cout << "Hello World!";
	cout << "Whattay!";
	cin.get(); // waits till you press "ENTER"
}